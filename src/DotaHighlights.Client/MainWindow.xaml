<Window x:Class="DotaHighlights.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DotaHighlights.Client.ViewModels"
        xmlns:views="clr-namespace:DotaHighlights.Client.Views"
        mc:Ignorable="d"
        Title="Highlights Store" Height="640" Width="960"
        Background="#0B0D12">
    <Window.Resources>
        <!-- Mapea cada ViewModel a su vista -->
        <DataTemplate DataType="{x:Type vm:StoreViewModel}">
            <views:StoreView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MainViewModel}">
            <views:CaptureView/>
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding CurrentView}"/>
</Window>
